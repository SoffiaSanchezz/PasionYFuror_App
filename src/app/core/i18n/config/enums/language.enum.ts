export enum Language {
    SPANISH = 'es',
    ENGLISH = 'en',
}

export enum LoadingState {
    IDLE = 'idle',
    LOADING = 'loading',
    SUCCESS = 'success',
    ERROR = 'error',
}

export enum TranslationNamespace {
    COMMON = 'common',
    EXAMPLE = 'example',
    AUTH = 'auth',
    NAVIGATION = 'navigation',
}
